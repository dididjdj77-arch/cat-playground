0) 결론 요약
문서 세트의 “골격”은 좋다

SSOT 계층(컨텍스트/인덱스/결정/오픈/플레이북) 구조는 이미 강하다.

그런데 디테일(키 목록/리턴 타입/조인 키)에서 드리프트가 이미 발생했고, 이게 런치 전 사고로 직결될 수 있다.

런치 블로커급 문서/설계 결함(P0)이 몇 개 있다

profiles.id vs profiles.user_id 조인 키 혼선: 예시 SQL이 D-094와 충돌(치명)

app_config 키(특히 popular_feed)의 SSOT 불일치: 여러 문서에서 키/whitelist가 충돌

payload_version_rollups의 unknown_count 누락 드리프트

moderation(신고) write RPC가 guard_terms_agreed / guard_block / 존재 은닉을 강제하지 않는 템플릿

공개 house summary RPC의 리턴 shape/404 처리 방식이 문서 간 불일치

이 5개는 “문서 수정 제안”이 아니라 즉시 SSOT 정리(문서+템플릿 동시 수정) 권고다.

1) 문서 수 축소: “역할 겹침 제거” 중심 통합안 (추천)

원칙: 필요한 내용을 삭제하지 않고, “같은 사실을 여러 문서가 말하는 구조”를 제거한다.

1-A. 통합/폐기(Stub로 이관) 제안 목록
(1) PACKET-TEMPLATES.md → PROCESS.md로 흡수 (문서 -1)

템플릿은 프로세스의 일부다. 별도 파일 유지 시, 프로세스 개정과 템플릿이 어긋난다.
조치: PROCESS에 “부록: Packet Templates” 섹션 추가 후 PACKET-TEMPLATES는 Status: MOVED → PROCESS#appendix로 3~4줄 stub만 남김.

(2) DOMAIN-MAP.md → ARCHITECTURE-OVERVIEW.md에 1섹션으로 흡수 (문서 -1)

DOMAIN-MAP은 “빠른 그림” 용도인데, ARCHITECTURE-OVERVIEW가 이미 1장 요약이다. 합치면 “세션 복구용 1장” 완성도가 올라간다.
조치: ARCHITECTURE-OVERVIEW에 “Domain relationships (1-page)”로 이동 후 DOMAIN-MAP은 stub.

(3) TESTING-STRATEGY.md + QA-SCENARIOS.md → VERIFICATION.md로 통합 (문서 -1 net)

현재는 게이트 정의/시나리오가 두 파일에 중복 분산되어 drift 위험이 크다.
조치:

VERIFICATION.md 구조:

§1 CI 필수(Gate + 테스트 목록 + 스냅샷)

§2 수동 QA(런치 전 체크)

§3 런치 체크리스트(현재 TESTING-STRATEGY에 임시로 있는 PITR/복구 항목 포함)

기존 두 문서는 stub 처리 + INDEX 갱신.

(4) API-CONTRACTS.md + RPC-SPECS.md → API.md로 통합 (문서 -1 net)

“논리 계약(REST)”과 “실제 RPC 시그니처/가드 패턴”이 분리된 건 이해되지만, 현재는 공통 가드/에러/리턴 타입이 문서 간 충돌한다. 통합해 SSOT를 하나로 줄이는 게 드리프트 비용을 줄인다.
조치: API.md 구조:

§0 Transport Adapter(error_code→HTTP) (기존 API-CONTRACTS)

§1 공통 Guard 함수/순서(SSOT) (기존 RPC-SPECS + D-096 링크)

§2 도메인별 Contract(관찰/냥스타/채널/하우스/운영)

§3 RPC signatures(실구현 인터페이스)

기존 API-CONTRACTS/RPC-SPECS는 stub.

(5) SCHEMA-MIGRATIONS.md는 “진행 체크리스트(TODO)” 성격이 강함 → 폐기/이관 (문서 -1)

PROCESS는 “TODO 문서 미사용” 원칙을 갖고 있는데, SCHEMA-MIGRATIONS는 체크박스 진행표로 drift/형식 위반 가능성이 높다.
권장 조치(안전):

“마이그레이션 순서(001~019)” 같은 불변 베이스라인만 남기고,

“완료/예정 체크박스”는 제거(또는 EP 결과로만 추적).

이 베이스라인은 playbooks/migrations.md 하단 “Baseline migration order” 섹션으로 옮기고, SCHEMA-MIGRATIONS는 stub 또는 삭제(정책에 맞춰 stub 권장).

(6) RLS-POLICY.md는 playbooks/rls-and-guards.md로 흡수 후 stub (문서 -1)

“RLS/가드 적용 실행 가이드”가 이미 playbook으로 존재하고, RLS-POLICY는 동일 원칙을 반복한다. 이중 SSOT가 된다.
조치: rls-and-guards playbook 상단에 “RLS baseline(원칙)” 섹션을 넣고, RLS-POLICY는 stub.

1-B. 축소 후 “타겟 SSOT 문서 세트(예시)”

(문서 수 감소 + drift 면적 축소)

CONTEXT / INDEX / DECISIONS / OPEN (유지)

PROCESS (PACKET 템플릿 흡수) 

PROCESS

ARCHITECTURE-OVERVIEW (DOMAIN-MAP 흡수) 

ARCHITECTURE-OVERVIEW

AUTHZ-MODEL (정책식 SSOT 유지) 

AUTHZ-MODEL

DATA-MODEL (스키마 기준) 

DATA-MODEL

CONFIG-BASELINES (수치 SSOT) 

CONFIG-BASELINES

API (API-CONTRACTS+RPC-SPECS 흡수)

ROUTES-AND-IA 

ROUTES-AND-IA

VERIFICATION (TESTING+QA 흡수)

HOWTO/local-setup (선택 유지) 

local-setup

playbooks/*는 유지(단, RLS-POLICY 흡수) 

rls-and-guards

2) 전수 조사: SSOT 위반/충돌/드리프트 (P0~P2)

아래는 “문서 정합” 문제가 아니라 구현자가 그대로 따라하면 깨지거나 누출될 가능성이 높은 지점들이다.

P0-1. profiles.id vs profiles.user_id 조인 키 혼선 (치명)

무슨 문제인가

D-094는 “도메인 전반 FK 기준은 auth.users.id(=profiles.user_id)로 통일”을 LOCK으로 선언한다.

그런데 공개 피드/댓글 예시 SQL이 profiles pr on pr.id = p.author_id 형태로 되어 있다. 이건 조인 불일치/닉네임 누락/권한 가드 파손으로 이어진다. 

rpc-public

moderation에서도 profiles pr on pr.id = r.reporter_id 형태가 동일하게 위험하다. 

moderation

왜 치명인가

잘못된 조인은 “데이터가 안 나온다” 수준이 아니라,

guard_block 대상 user_id 해석이 꼬이면 차단/노출 정책이 깨질 수 있고

작성자 식별(nickname)도 깨진다.

권장 수정(문서/템플릿 즉시 수정)

모든 예시/템플릿에서 아래로 통일:

profiles.user_id = <author_id/owner_id/reporter_id/...>

profiles.id는 “프로필 자체 내부 PK”로만 쓴다.

P0-2. app_config 키/whitelist의 SSOT 충돌 (popular_feed) (치명)

증상

CONFIG-BASELINES는 app_config 키 매핑에 popular_feed를 포함한다. 

CONFIG-BASELINES

DECISIONS D-076도 popular 피드가 popular_feed로 런타임 조정된다고 한다. 

DECISIONS

그런데 ops-app-config playbook / RPC-SPECS / RLS-POLICY는 whitelist를 3개 키로만 제한한다(= popular_feed 읽기 불가).

왜 치명인가

런치 후 운영 중에 “popular 피드 튜닝”을 app_config로 하겠다고 LOCK 해놓고, 실제 읽기 경로에서 키가 막히면 운영이 멈춘다.

권장 수정(SSOT를 하나로 만들기)

키 목록은 CONFIG-BASELINES §3을 SSOT로 고정하고,

DECISIONS/D-056 같은 곳에서 “키 리스트를 직접 열거”하지 말고 링크만 남겨 드리프트 면적을 줄인다.

rpc_get_app_config whitelist에 popular_feed 추가 + seed도 포함(값은 CONFIG-BASELINES 기준).

P0-3. payload KPI: rollup에서 unknown_count 누락 드리프트 (치명)

증상

D-089는 unknown payload_version을 ACCEPT하고, rollups에 unknown_count를 두라고 명시한다.

그런데 payload-version-kpi playbook의 롤업 SQL 템플릿은 unknown_count 집계를 하지 않는다. 

payload-version-kpi

왜 치명인가

unknown 처리를 “운영 가능한 시스템”으로 만든다고 했는데(정책), 집계가 누락되면 운영이 눈이 멀게 된다(새 버전 유입 감지가 안 됨).

권장 수정

playbook 롤업 insert에 unknown_count 포함 + event_type='unknown' count 반영.

P0-4. moderation 신고(write) 템플릿이 guard_terms / guard_block / 존재은닉을 강제하지 않음 (치명)

증상

D-073: 모든 write RPC는 guard_terms_agreed() 필수. 

DECISIONS

D-019: block 관계에서는 report도 허용하지 않는다(guard_block 적용).

그런데 moderation playbook의 rpc_report_content 템플릿은:

guard_terms_agreed 호출 없음

target author 기준 guard_block 없음

“차단/비공개/삭제/숨김 상태”에서의 존재 은닉(not_found/404) 통일 전략이 없음 

moderation

왜 치명인가

약관 미동의 계정도 신고/자동숨김 트리거에 영향을 주면 운영 사고/어뷰즈 가능성이 올라간다.

차단 상태에서 report가 가능하면, 에러 응답 차이로 존재 여부 누출이 생길 수 있다(D-050 류). 

DECISIONS

권장 수정(문서 템플릿 레벨에서 강제)

report RPC도 “write guard 순서(LOCK)” 적용:
guard_terms_agreed → guard_block → guard_soft_state(대상) → (필요시) visibility/published

구현을 위해 각 target_type별로 author_id(owner_id)를 조회해 guard_block을 평가해야 한다(단순 snapshot만으로는 불가).

차단/숨김/삭제/존재없음 모두 error_code='not_found'로 통일(외부 표면 404).

P0-5. 공개 House Summary RPC: 리턴 타입/404 처리/빈 슬롯 표현이 문서 간 불일치 (치명)

증상

RPC-SPECS는 rpc_get_public_house_slots_summary가 jsonb 리턴 + not_found를 전제로 설명한다.

rpc-public playbook 템플릿은 RETURNS TABLE 형태(행 반환)다. 

rpc-public

왜 치명인가

house는 “빈 슬롯도 포함” 정책이 있고(요약 DTO), 리소스가 존재하지만 비어 있는 상태와 “not_found(가드 실패)”를 구분해야 한다.

TABLE 반환으로 0 rows가 나오면 “정말 빈 하우스인지 / not_found인지”가 애매해질 수 있다(특히 slots seed가 없는 경우).

권장 수정(SSOT 통일)

공개 house summary는 반드시 jsonb로 통일 권장:

성공: { "slots": [ {slot_key, equipped_at, type, standard_name}, ... ] }

실패: { "error_code": "not_found" }

또한 slot_key는 v1에 8개 고정이므로, DB에서 “slot_01..slot_08”을 생성해 left join 해서 항상 8개를 반환하도록 하면 “빈 하우스 vs not_found”가 명확해진다.

P1. 중복/역할 겹침으로 인한 drift 위험(정리 필요)
(1) ARCHITECTURE-OVERVIEW가 AUTHZ 정책식을 사실상 복붙하고 있음

ARCHITECTURE-OVERVIEW “공개 노출은 항상 …”은 AUTHZ-MODEL §0-1을 재서술한다.
조치: 해당 문장을 링크로 대체(“See AUTHZ-MODEL §0-1”)하고, overview는 “구성/도메인/플로우”만 남기는 게 SSOT 위반을 줄인다.

(2) rpc_get_app_config 예시 seed가 “샘플”로 너무 축약되어 실수 유도

ops-app-config의 seed 예시는 rate_limits를 posts만 넣는다. 런치에서 그대로 쓰면 정책 위반.
조치: “샘플”이 아니라 CONFIG-BASELINES와 1:1 정합인 full seed를 제공하고, 샘플은 별도 주석 블록으로 격리.

(3) API-CONTRACTS의 “Public DTO에는 inventory 관련 필드를 어떤 형태로도 포함하지 않는다” 문구가 과도

house 요약은 type/standard_name을 포함한다(D-055). ID/raw/meta 금지와 ‘파생 속성’ 허용을 분리해야 혼선이 줄어든다.

P2. 참조 링크/문장 단위 품질(사고는 아니지만, 지속적 피로를 유발)

D-043이 참조로 rpc-public을 찍는데, 의미상 owner write(슬롯 바인딩)에 더 가깝다. 링크 정리 권장.

OPEN/DECISIONS/PLAYBOOK 간에 “키 목록을 직접 열거”하는 부분들이 많다 → 링크로 통일하는 방향이 장기적으로 안정적이다.

3) 설계 점검: 누락/모순/구현 리스크
3-A. 누락(런치 전 반드시 메워야 할 공백)
(1) 레이트리밋 “어디에서/어떻게” enforced 되는지 SSOT가 없음

수치(CONFIG-BASELINES)와 원칙(D-053)은 있는데,

DB RPC에서 체크할지

Edge Function에서 체크할지

어떤 저장소(카운터 테이블? sliding window?)를 쓸지

위반 시 에러코드/HTTP 매핑이 뭔지
가 없다.

권장: ops-app-config/playbook 또는 moderation playbook에 “rate limit enforcement 패턴”을 추가해 런치 공백을 막아야 한다(문서 추가 대신 기존 playbook 흡수 권장).

(2) 썸네일/ISR 재검증(D-072)의 실행체(웹훅/엣지) 사양이 없음

D-072는 “비동기 수행”이라고만 하고, 어떤 트리거/재시도/실패 처리가 없다.
권장: seo-web playbook에 “revalidate API + thumbnail worker(Edge Function) 계약/실패 시 재처리” 섹션을 추가(또는 별도 playbook이 필요하면, 다른 문서를 통합해 총량을 맞추는 방식).

(3) pg_cron 등록/검증/실패 관측 Runbook이 없음

CONFIG-BASELINES에 cadence는 있지만, 실제 job 등록 SQL, job 이름 규칙, 실패 시 재시도/알림(최소한의 관측)이 없다.

(4) Storage bucket/policy 적용/검증 playbook 부재

스키마/결정에는 버킷/경로/정책이 있는데, “어떻게 검증할지”가 없다.

3-B. 구현자가 실수하기 쉬운 지점(사고 유발 포인트)

SECURITY DEFINER 함수에서의 조인 키 혼선(이미 P0로 발견)

TABLE 반환 vs JSON 반환 혼용: error_code 패턴(D-065) 적용이 어려워짐

“빈 결과=404?” 판정 로직이 외부 표면(SSR/SDK)에 퍼질 위험: 404 통일(D-050)을 지키려면, DB 레벨에서 not_found를 명시적으로 반환하는 편이 안전하다.

config seed가 샘플인 상태로 굳을 위험: 런치 후 운영 튜닝 이전에 이미 잘못된 값으로 고착

4) Upside ↑ / Downside ↓ 제안 (문서/설계/검증을 같이 올리는 방향)
4-A. Downside(사고/누출/오염) 감소: “드리프트 자동 차단”

문서-코드 드리프트를 CI에서 잡는 최소 규칙

SQL 템플릿/예시에서 profiles.id = <user_id> 패턴을 금지하는 grep/lint(단순하지만 효과 큼)

app_config whitelist 키 목록이 CONFIG-BASELINES §3과 동일한지 스냅샷 테스트

payload_version_rollups 컬럼/쿼리에 unknown_count가 포함되는지 스냅샷 테스트
(이건 “새 문서 추가”가 아니라 테스트 추가로 drift를 줄인다.)

공개 DTO whitelist 테스트를 ‘템플릿화’

이미 Gate(G-2)가 있으니, house/posts/threads 공통으로 “금지 필드 목록”을 상수화해 재사용하면 확장 속도가 올라간다.

4-B. Upside(속도/확장/생산성) 증가: “SSOT를 더 얇게, 실행을 더 빠르게”

키 목록/수치/정책식을 ‘한 곳만’

키 목록: CONFIG-BASELINES만

정책식: AUTHZ-MODEL §0만

에러 매핑: API(Transport Adapter)만
나머지는 링크만 유지 → 문서 수정 비용이 급감한다.

예시 SQL은 “정합성 보장된 최소 1개”만 남기고 나머지 제거

예시는 복붙되기 때문에 drift가 곧 버그가 된다.

예시는 1개만 “golden template”로 유지하고, 나머지는 링크/원칙으로 정리하는 편이 결과적으로 속도가 난다.

5) “서비스 런치까지” 관점의 공백 체크리스트 (현재 SSOT로는 비는 것)

여기서 말하는 “공백”은 기능이 아니라 운영 가능한 출시 관점이다.

5-A. 릴리즈/롤백/복구

DB 마이그레이션 롤백은 skeleton은 있으나, **프로덕션 적용 절차(백업/PITR 확인/다운타임 판단/락 타임아웃 표준)**가 “런치 체크리스트”로 정리되어 있지 않다.

5-B. 운영 관측(Observability)

pg_cron/썸네일 워커/cleanup/rollup 등 백그라운드 작업의 실패를 어떻게 감지/재시도할지 문서가 없다.

5-C. 보안 점검

SECURITY DEFINER 함수/권한(REVOKE/GRANT)/search_path 고정의 “릴리즈 전 점검표”가 없다. (각 playbook에 흩어져 있음)

5-D. 스토리지 정책 검증

버킷 정책이 “설계”로만 있고, “검증 커맨드/시나리오(anon read 가능한지/원본이 안 새는지)”가 없다.

6) 즉시 실행 권고: “문서 리뉴얼 EP”로 쪼갠다면 (제안)

질문 없이, 바로 실행 가능한 단위로 쪼개면 아래 순서가 합리적이다(드리프트 차단 우선).

EP-A: P0 드리프트 5건 패치(문서+템플릿)

profiles join 키 정정

app_config popular_feed 정합(whitelist/seed/SSOT)

payload rollup unknown_count 반영

moderation report 템플릿에 guard_terms/guard_block/존재은닉 패턴 반영

house public summary jsonb + 8-slot 안정 shape 통일

EP-B: 문서 통합(문서 수 축소)

PACKET→PROCESS

DOMAIN-MAP→ARCH-OVERVIEW

TESTING+QA→VERIFICATION

API-CONTRACTS+RPC-SPECS→API

SCHEMA-MIGRATIONS/RLS-POLICY 흡수+stub

EP-C: 런치 공백 메우기(문서 추가 없이 기존 playbook 확장)

rate limit enforcement 패턴

thumbnail/ISR worker 계약/실패 처리

pg_cron runbook + 관측/재시도

storage 정책 검증 시나리오

D-094(유저 식별자 키) 위반을 실사례로 적발: profiles.id vs profiles.user_id 조인 혼동이 실제로 “작성자 매핑 오류/0 rows/닉네임 오노출”로 이어질 수 있다는 경고 + 수정안 제시. 

app_config 키 드리프트(popular_feed) 탐지: CONFIG-BASELINES/DECISIONS에는 있는데 whitelist(playbook/RPC-SPECS)에서 빠진 상태를 “의도(운영 조정 가능) vs 구현(조회 불가)”로 명확히 규정. 

house_slots_summary의 타입/의미 흔들림 지적: RETURNS TABLE vs RETURNS jsonb 충돌 + “접근불가 vs 빈 슬롯” 의미가 섞이는 위험을 짚고, 2-step(부모 가드 → 슬롯 조회) 패턴을 권고. 

cats 아바타 누출 방지 범위가 avatar_url에만 치우친 문제: 정책상 avatar_key도 금지인데 테스트/체크리스트가 avatar_url만 반복 언급하는 drift를 지적. 

드리프트 “탐지 장치”의 필요성을 문서가 아니라 CI/정적검증으로 제안: 규칙이 잘 써 있어도 실제로는 어긋난다는 점(C-1~C-4)과, 이를 잡는 최소 자동화(키 정합/조인 규약/DTO 누출)를 제안한 게 실효성이 높았음. 

온보딩 deadlock(terms 가드) 리스크 제기: “모든 write는 terms 필요” 원칙이, 약관동의 자체/초기 닉네임 설정 같은 bootstrap write와 충돌할 수 있음을 명시적으로 드러냄. 

SEO 캐시 vs block(로그인 뷰어 기준) 결합의 잠재 누출: ISR 캐시가 anon 기준으로 생성될 때, 로그인 사용자에게 block 반영 기대가 깨질 수 있다는 설계 공백 지적. 

payload_version_rollups의 unknown_count 불일치, invalid_request 매핑 누락 같은 “작은데 치명적인 정합성”도 잡아줌. 

